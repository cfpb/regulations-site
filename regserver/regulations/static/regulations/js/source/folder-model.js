// one instance per folder type: sxs, permalinks, etc
define('folder-model', ['underscore', 'backbone', 'meta-model'], function(_, Backbone, MetaModel) {
    'use strict';

    var FolderModel = MetaModel.extend({
        
    });

    return FolderModel;
});
