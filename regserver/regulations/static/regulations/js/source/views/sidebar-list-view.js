// **Extends** Backbone.View
//
// **Usage** ```require(['sidebar-list-view'], function(SidebarListView) { var thing = SidebarListView.extend({}); })```
define('sidebar-list-view', ['jquery', 'underscore', 'backbone', 'folder-model'], function($, _, Backbone, FolderModel) {
    'use strict';
    var SidebarListView = Backbone.View.extend({
        initialize: function() {
            
        }
    });

    return SidebarListView;
});
